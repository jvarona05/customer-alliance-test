nelmio_api_doc:
    documentation:
        info:
            title: Customer Alliance Test
            description: Technical test
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
            - ^/widget(?!/doc$) # Accepts routes under /api except /api/doc
